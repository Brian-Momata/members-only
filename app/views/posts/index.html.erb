<h1>Posts from members</h1>
<% if @posts.empty? %>
  <p>There are no posts available</p>
<% else %>
  <ul>
  <%= @posts.each do |post| %>
    <li><%= post.body %></li>
  <% end %>
  </ul>
<% end %>

<%= link_to 'create new post', new_post_path %>